(ns aoc2022.day00
  (:require [clojure.set :as set]
            [clojure.string :as str]))

(defn input-lines []
  (line-seq (java.io.BufferedReader. *in*)))

(defn run [args]
  (let [a 1]
    (println "star 1:")
    (println "star 2:"))
