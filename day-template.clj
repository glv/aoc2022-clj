(ns aoc2022.day00
  (:require [clojure.set :as set]
            [clojure.string :as str]))

(defonce input-lines
  (line-seq (java.io.BufferedReader. *in*)))

(defn run [args]
  (let [a line-seq]
    (println "star 1:")
    (println "star 2:"))
